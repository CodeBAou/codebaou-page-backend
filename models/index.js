const model_users     = require('./users');
const model_post      = require('./post');
const model_section   = require('./sections');

module.exports = {
    model_users,
    model_post,
    model_section
}